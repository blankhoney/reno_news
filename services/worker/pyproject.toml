[project]
name = "reno-news-worker"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "psycopg[binary]>=3.2.0",
]

[project.scripts]
reno-worker = "reno_worker.server:main"

[tool.uv]
package = true
