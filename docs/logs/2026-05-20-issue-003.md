# 2026-05-20 Issue 003 Log

## Objective

Implement `docs/CODEX_MASTER_PLAN.md` Issue 003: Source Registry and Source Policy foundation.

## Research

- Queried Fastify documentation for JSON Schema route validation and `app.inject` tests.
- Queried node-postgres documentation for parameterized queries, `pool.query`, and transaction client handling.
- Queried Psycopg 3 documentation for synchronous connections and dictionary row factories.
- Queried PostgreSQL documentation for `ALTER TABLE`, check constraints, foreign keys, and unique constraints.

## Design Decision

Extend the existing `sources` table into the Source Registry and store Source Policy in a separate one-to-one `source_policies` table. Keep RSS ingest out of scope.

## Implementation Notes

- Added migration `0002_source_registry_policy.sql`.
- Renamed the early `sources.kind` field to `sources.source_type`.
- Added `source_policies` with crawl, rate-limit, save-level, rights, translation, and risk fields.
- Updated development seed data to create policies for the five MVP sample sources.
- Added `packages/db` Source Repository functions for list/create/update and worker-readable policy rows.
- Added Fastify `GET /sources`, `POST /sources`, and `PATCH /sources/:id` routes with JSON Schema validation.
- Added Python worker Source Policy reader using Psycopg 3.
- Added `DATABASE_URL` to Compose API, worker, and scheduler environments.

## Verification

- Confirmed API red tests failed before route implementation.
- Confirmed DB integration red tests failed before Source Repository implementation.
- Confirmed worker red test failed before Source Policy reader implementation.
- `pnpm install --frozen-lockfile`
- `pnpm lint`
- `pnpm test`
- `pnpm build`
- `pnpm --filter @reno-news/db test:integration`
- `uv run python -m unittest discover -s tests`
- `DATABASE_URL=postgres://reno_news:reno_news@localhost:5432/reno_news_issue003_test uv run python -m unittest tests/test_source_policy.py`
- Fresh test database migration and repeated seed verified 5 boards, 5 sources, 5 source policies, 5 raw entries, and 2 migrations.
- Temporary real API process verified `GET /sources`, `POST /sources`, and `PATCH /sources/:id` against PostgreSQL.
- `docker compose -f infra/compose/compose.yml up -d --no-build`
- Compose health checks for web, API, worker, PostgreSQL, Redis, Caddy API proxy, and Caddy worker proxy.

## Verification Limitation

`docker compose up --build -d` could not complete because the local Docker credential helper stalled and an anonymous Docker Hub token request returned EOF when bypassing the helper with a temporary `DOCKER_CONFIG`. The new Source API was verified through a real local API process against PostgreSQL; Compose service health was verified with existing images using `--no-build`.
