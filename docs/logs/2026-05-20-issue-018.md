# 2026-05-20 Issue 018 Log

## Objective

Plan the next Milestone 6 slice: lightweight reader-safe Related Items without feedback-to-ranking, personalization, digest generation, semantic search, or external search services.

## Research

- Re-read Milestone 6 focus and current next action in `docs/CODEX_MASTER_PLAN.md`.
- Re-read local domain language for Reader, Reader Detail Projection, Reader Search Query, Reader Search Result, Feedback, Digest, Board, Source, Rights Policy, and Summary Block.
- Re-read the current ADR list, especially PostgreSQL-first search and reader-safe search projection decisions.
- Queried current PostgreSQL 18 docs through Context7 for full-text rank, weighted `tsvector`, and `ts_rank_cd` guidance.
- Queried current Next.js 16.2.2 docs through Context7 for App Router dynamic pages, `notFound`, and `Link`.
- Queried current Fastify docs through Context7 for v5 full JSON Schema route validation.

## Design Decision

Start Related Items as a small reader-detail affordance selected from the same policy-visible PostgreSQL projection used by reader listings, detail pages, and search. Related Items may use shared board, shared source, recency, and reader-safe PostgreSQL full-text rank, but must not consume feedback, personal saved/read-later state, digest membership, private extraction text, translation drafts, semantic vectors, or external search services.

## Implementation Notes

- Added Related Item terminology to `CONTEXT.md`.
- Added ADR 0022 for reader-safe PostgreSQL related item selection.
- Added `docs/architecture/issue-018-plan.md`.
- Updated `docs/CODEX_MASTER_PLAN.md` and the goal tracker so Issue 018 implementation became the next concrete action.
- Added DB integration coverage for related item visibility, self exclusion, same-board and text-match behavior, hidden raw entries, blocked items, disabled-source items, missing targets, bounded limits, and empty results.
- Added `ReaderRepository.listRelatedReaderItems` over the existing reader-safe PostgreSQL projection.
- Added `GET /reader/items/:id/related` with Fastify v5 full JSON Schema params/query validation.
- Added web related-items API helper tests and rendered related item cards on reader item detail pages.
- Updated README and Reader API docs for related items.

## Verification

- `docs/architecture/issue-018-plan.md` maps Related Items to concrete tasks and acceptance criteria.
- ADR 0022 records the reader-safe PostgreSQL boundary.
- `CONTEXT.md` defines Related Item and separates it from personalized recommendations, ranking signals, and digest items.
- `docs/CODEX_MASTER_PLAN.md` marks Issue 018 done, points to next Milestone 6 issue planning, and does not start feedback-to-ranking consumption, moderation workflow, digest generation, semantic/vector search, external search service, search extension deployment, auth/RBAC, backend personal-state sync, browser automation, or non-RSS adapters.
- `pnpm --filter @reno-news/db test:integration`
- `pnpm --filter @reno-news/api test`
- `pnpm --filter @reno-news/web test`
- `pnpm install --frozen-lockfile`
- `pnpm lint`
- `pnpm test`
- `pnpm build`
- `uv --project services/worker run python -m unittest discover -s services/worker/tests`
- `uv lock --check`
- Compose service status check for web, API, worker, scheduler, PostgreSQL, Redis, and Caddy.
- Direct health smoke for web, API, and worker.
- Caddy-proxied health smoke for web, API, and worker.
- Local current-code smoke on API `3101` and web `3100`:
  - Inserted a temporary reader-visible related-items fixture.
  - Verified `GET /reader/items/:id/related?limit=3` returned the related candidate.
  - Verified the reader item detail page rendered `Related items` and the related candidate card.
  - Deleted the smoke fixture after verification.
- Deferred-scope scan found only documentation boundary references and one existing worker test string for browser automation; no deferred implementation was added.
- Confirmed no related-items smoke rows remain and ports `3100` and `3101` are not listening.
- Rebuilt web after local dev smoke so `apps/web/next-env.d.ts` points back to production route types.
