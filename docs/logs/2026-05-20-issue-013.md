# 2026-05-20 Issue 013 Log

## Objective

Add the first Milestone 5 admin UI slice: editing the current Source Policy from the admin source detail page.

## Research

- Re-read Milestone 5 scope in `docs/CODEX_MASTER_PLAN.md`.
- Re-read local domain language around Admin, Admin Debug Surface, Source Policy, Rights Policy, Save Level, and Risk Level.
- Queried current Next.js App Router docs through Context7 for Server Actions, forms, `FormData`, `revalidatePath`, and `redirect`.
- Checked existing admin source detail, admin Server Actions, admin API client, Fastify source update route, and DB Source Repository.

## Design Decision

Use the existing source update path for policy edits. Issue 013 should mutate the current Source Policy through `PATCH /sources/:id`; it should not add a policy history table, approval workflow, auth/RBAC, or a separate CMS model.

## Implementation Notes

- Added Issue 013 plan.
- Added ADR 0017 for the policy edit mutation boundary.
- Added Policy Change terminology.
- Updated the master plan and goal tracker so Issue 013 implementation is next.

## Verification

- Planning artifacts exist for Issue 013.
- ADR 0017 records the source policy mutation boundary.
- `CONTEXT.md` defines Policy Change and relates it to Source Policy and Rights Policy.
- `docs/CODEX_MASTER_PLAN.md` points to Issue 013 implementation and does not start failure queue, feedback handling, raw-entry hide/restore, auth/RBAC, search, digest generation, browser automation, or non-RSS adapters.
