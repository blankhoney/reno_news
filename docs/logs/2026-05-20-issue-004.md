# 2026-05-20 Issue 004 Log

## Objective

Implement `docs/CODEX_MASTER_PLAN.md` Issue 004: minimal RSS/Atom adapter ingest flow.

## Research

- Queried feedparser documentation for RSS/Atom entry fields and bozo parse errors.
- Queried HTTPX documentation for synchronous GET, timeout, `raise_for_status`, and request/status errors.
- Queried Dramatiq documentation for Redis broker configuration, actors, and `.send()`.

## Design Decision

Run RSS/Atom ingest in the Python worker using HTTPX, feedparser, and Dramatiq. Store discovered feed metadata in `raw_entries` and store execution outcomes in `source_ingest_attempts`.

## Implementation Notes

- Added migration `0003_source_ingest_attempts.sql`.
- Added Python RSS/Atom ingest using HTTPX and feedparser.
- Added URL fragment normalization and SHA-256 canonical hashes.
- Inserted feed metadata into `raw_entries` with `metadata_ingested` processing stage.
- Used `on conflict do nothing` to avoid duplicate raw entries.
- Recorded success, skipped, and failure outcomes in `source_ingest_attempts`.
- Added Dramatiq `ingest_source_actor` and scheduler trigger for enabled RSS/Atom sources.
- Added `reno-scheduler` script entry point.

## Verification

- Confirmed RSS/scheduler red tests failed before implementation.
- `DATABASE_URL=postgres://reno_news:reno_news@localhost:5432/reno_news_issue004_test pnpm db:migrate`
- `DATABASE_URL=postgres://reno_news:reno_news@localhost:5432/reno_news_issue004_test pnpm db:seed`
- `DATABASE_URL=postgres://reno_news:reno_news@localhost:5432/reno_news_issue004_test uv run python -m unittest tests/test_rss_ingest.py tests/test_scheduler.py`
- Verified sample feed inserted 2 raw entries and duplicate ingest inserted 0 additional rows.
- Verified disabled source was skipped without fetch.
- Verified fetch failure recorded `failure_type = network`.
- Verified scheduler trigger enqueues only RSS/Atom source types.
- `pnpm install --frozen-lockfile`
- `pnpm lint`
- `pnpm test`
- `pnpm build`
- `pnpm --filter @reno-news/db test:integration`
- `uv run python -m unittest discover -s tests`
