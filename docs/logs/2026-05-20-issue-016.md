# 2026-05-20 Issue 016 Log

## Objective

Add the first Milestone 6 slice: reader-safe PostgreSQL search foundation.

## Research

- Re-read Milestone 6 scope in `docs/CODEX_MASTER_PLAN.md`.
- Re-read local domain language around Reader, Reader Item Card, Reader Detail Projection, Rights Policy, and Manual Moderation Action.
- Re-read ADR 0005 for PostgreSQL-first search and no default external search service.
- Checked current reader repository filters for enabled sources, hidden raw entries, and blocked rights status.
- Queried current PostgreSQL 18 docs through Context7 for `tsvector`, `websearch_to_tsquery`, `@@`, `ts_rank`, and GIN index guidance.

## Design Decision

Start with reader-safe PostgreSQL search over the existing reader projection. Issue 016 should not search private extraction text or translation draft full text, and should not add Meilisearch, OpenSearch, `pg_trgm`, `zhparser`, semantic search, feedback logic, or digest generation.

## Implementation Notes

- Added Reader Search Query and Reader Search Result terminology.
- Added ADR 0020 for the reader-safe PostgreSQL projection boundary.
- Added Issue 016 plan.
- Updated the master plan and goal tracker so Issue 016 implementation is next.

## Verification

- `docs/architecture/issue-016-plan.md` exists and maps PostgreSQL reader search to concrete implementation tasks and acceptance criteria.
- ADR 0020 records the reader-safe PostgreSQL search boundary.
- `CONTEXT.md` defines Reader Search Query and Reader Search Result.
- `docs/CODEX_MASTER_PLAN.md` points to Issue 016 implementation and does not start feedback handling, digest generation, semantic/vector search, external search service, search extension deployment, auth/RBAC, backend personal-state API, browser automation, or non-RSS adapters.
