# 2026-05-20 Issue 012 Log

## Objective

Add the next Milestone 4 reader UI slice: local saved and read-later personal-space behavior.

## Research

- Re-read Milestone 4 scope in `docs/CODEX_MASTER_PLAN.md`.
- Re-read local domain language around Reader, Personal Space, Saved Item, Read Later Item, Reader Item Card, and Reader Detail Projection.
- Queried current Next.js App Router docs through Context7 for Client Components, the `'use client'` directive, browser API boundaries, and serializable Server-to-Client props.

## Design Decision

Use local browser state for saved/read-later in MVP v0.1. The project has no auth or reader account model yet, so server-side personal state would introduce decisions outside the current milestone.

## Implementation Notes

- Added Issue 012 plan.
- Added ADR 0016 for local browser personal state.
- Added Saved Item and Read Later Item terminology.
- Updated the master plan and goal tracker so Issue 012 implementation is next.
- Added pure local personal-state helpers and tests.
- Added Client Component save/read-later controls.
- Added controls to reader cards, board lists, and item detail pages.
- Added `/personal` local saved/read-later page.
- Added local personal-state documentation and README endpoint notes.

## Verification

- Planning artifacts exist for Issue 012.
- ADR 0016 records the local browser-state boundary.
- `CONTEXT.md` defines Saved Item and Read Later Item.
- `docs/CODEX_MASTER_PLAN.md` closed Issue 012, marked Milestone 4 done, and points to Milestone 5 planning without starting auth, backend personal-state APIs, ranking changes, search, digest generation, public publishing workflow, browser automation, or non-RSS adapters.
- Red test first failed for missing `personalState` module.
- `pnpm --filter @reno-news/web test`
- `pnpm --filter @reno-news/web lint`
- `pnpm --filter @reno-news/web build`
- `pnpm install --frozen-lockfile`
- `pnpm lint`
- `pnpm test`
- `pnpm build`
- `uv run python -m unittest discover -s tests`
- `uv lock --check`
- `docker compose -f infra/compose/compose.yml ps`
- Direct and Caddy-proxied health smoke for web, API, and worker.
- Headless Chrome CDP smoke verified Save and Read later clicks write localStorage, `/personal` renders both lists, and state persists after reload.
