# 2026-05-20 Issue 015 Log

## Objective

Add the next Milestone 5 admin UI slice: manual hide and restore for raw entries.

## Research

- Re-read Milestone 5 scope in `docs/CODEX_MASTER_PLAN.md`.
- Re-read local domain language around Admin, Admin Debug Surface, Candidate Item, Published Item, and Feedback.
- Checked `raw_entries.lifecycle_status` constraints and confirmed `hidden` already exists.
- Checked the current raw-entry repository, API routes, admin raw-entry detail page, and reader repositories.
- Queried current Next.js docs through Context7 for Server Action form mutation, `revalidatePath`, and `redirect` behavior.
- Queried current Fastify docs through Context7 for v5 full JSON Schema route validation.

## Design Decision

Use the existing raw-entry lifecycle state for manual hide/restore. Issue 015 should set `hidden` to remove an item from reader-facing surfaces and restore to `candidate` because the MVP does not store previous lifecycle history.

## Implementation Notes

- Added Manual Moderation Action terminology.
- Added ADR 0019 for the raw-entry lifecycle boundary.
- Added Issue 015 plan.
- Added DB integration tests for hide, restore, missing raw entry, and reader hidden filtering.
- Added `RawEntryRepository.updateRawEntryLifecycle`.
- Added `PATCH /raw-entries/:id` with Fastify v5 params and body schema validation.
- Added web helper tests, lifecycle action helper, Server Action, and hide/restore form on `/admin/raw-entries/[id]`.
- Updated README, Raw Entries API docs, Reader API docs, master plan, and goal tracker.

## Verification

- `docs/architecture/issue-015-plan.md` exists and maps manual hide/restore to concrete implementation tasks and acceptance criteria.
- ADR 0019 records the decision to reuse current raw-entry lifecycle state.
- `CONTEXT.md` defines Manual Moderation Action and distinguishes it from Feedback, delete, retry, and source policy changes.
- Red DB test first failed for missing `updateRawEntryLifecycle`.
- Red API test first failed with `PATCH /raw-entries/:id` returning 404.
- Red web test first failed for missing lifecycle helper functions.
- `DATABASE_URL=postgres://reno_news:reno_news@localhost:5432/reno_news pnpm --filter @reno-news/db test:integration`
- `pnpm --filter @reno-news/api test`
- `pnpm --filter @reno-news/web test`
- `pnpm install --frozen-lockfile`
- `pnpm lint`
- `pnpm test`
- `pnpm build`
- `uv --project services/worker run python -m unittest discover -s services/worker/tests`
- `uv lock --check`
- `docker compose -f infra/compose/compose.yml ps`
- Direct and Caddy-proxied health smoke for web, API, and worker.
- Local dev smoke submitted Hide and Restore through the raw-entry detail Server Action at `http://localhost:3100/admin/raw-entries/1`.
- Local smoke verified `GET /reader/items/1` returned 404 while hidden and 200 after restore.
- Reset the smoke seed entry lifecycle back to `new`.
- `docs/CODEX_MASTER_PLAN.md` closes Issue 015, marks Milestone 5 done, and does not start feedback handling, moderation history, bulk moderation, delete flow, retry/resolution workflow, source creation UI, policy history, auth/RBAC, search, digest generation, browser automation, or non-RSS adapters.
