# 2026-05-20 Issue 020 Log

## Objective

Plan and implement the next Milestone 6 slice: a bounded Quality Feedback Penalty for Digest preview ordering.

## Research

- Re-read Issue 017 feedback capture, ADR 0021, Issue 019 Digest preview, and ADR 0023.
- Re-read `docs/research/deep-research-report.md` sections on non-personalized high-value push and quality feedback penalties.
- Checked PostgreSQL 18 docs for aggregate behavior, `CREATE VIEW` tradeoffs, and bounded `SELECT` ordering/limits.
- Checked Fastify v5 guidance that route schemas need full JSON Schema when query validation changes.
- Checked Next.js 16.2.2 App Router docs for server `searchParams` and `Link`; Issue 020 does not need a new client-state boundary.

## Design Decision

Issue 020 should consume feedback only as a computed ordering penalty for Digest preview. The penalty is bounded, explainable, and non-moderating. It does not create reader accounts, trust weighting, moderation resolution state, automatic hide/restore, persisted digest editions, or new ranking tables.

## Implementation Notes

- Added Quality Feedback Penalty terminology to `CONTEXT.md`.
- Added ADR 0024 for bounded, non-moderating feedback consumption.
- Added `docs/architecture/issue-020-plan.md`.
- Updated `docs/CODEX_MASTER_PLAN.md` and the goal tracker so Issue 020 implementation became the next concrete action.
- Added DB integration coverage proving Digest ordering lowers otherwise comparable items with higher Quality Feedback Penalty.
- Covered penalty clamping and all current Feedback Types.
- Reused existing visibility coverage for hidden raw entries, blocked items, and disabled-source items.
- Implemented the computed, clamped penalty inside `ReaderRepository.listReaderDigestItems`.
- Kept `GET /reader/digest` response shape unchanged.
- Updated README and Reader API docs for the digest ordering signal.

## Verification

- `docs/architecture/issue-020-plan.md` maps the feedback penalty slice to concrete tasks and acceptance criteria.
- ADR 0024 records that the first feedback consumption path is bounded, digest-only, and non-moderating.
- `CONTEXT.md` defines Quality Feedback Penalty and separates it from votes, personal preferences, hide rules, and moderation workflow.
- `docs/CODEX_MASTER_PLAN.md` marks Issue 020 done, points to the next Milestone 6 issue planning step, and does not start moderation workflow, feedback resolution, reader identity, trust weighting, backend personal-state sync, semantic/vector search, external search service, search extension deployment, digest delivery, persisted digest table, editorial workflow, browser automation, or non-RSS adapters.
- Red test: `pnpm --filter @reno-news/db test:integration` failed before implementation because the clean item did not rank ahead of the penalized item.
- Green test: `pnpm --filter @reno-news/db test:integration`
- `pnpm --filter @reno-news/api test`
- `pnpm install --frozen-lockfile`
- `pnpm lint`
- `pnpm test`
- `pnpm build`
- `uv --project services/worker run python -m unittest discover -s services/worker/tests`
- `uv lock --check` from `services/worker`
- Compose service status plus direct and Caddy-proxied health checks for web, API, and worker.
- Current-code smoke on API `3101` confirmed a clean digest item ranked before an otherwise comparable item with all current Feedback Types.
- Current-code smoke on web `3100` confirmed `/digest?board=ai` rendered the clean item before the penalized item.
- Deleted Issue 020 smoke rows and confirmed no smoke raw entries remain.
- Stopped local smoke servers and confirmed no listeners remained on `3100` or `3101`.
- Rebuilt web after the local dev smoke and confirmed `apps/web/next-env.d.ts` has no diff.
- Deferred-scope scan found only documentation boundary references plus one existing worker test string for browser automation; no deferred implementation was added.
