# 2026-05-20 Issue 002 Log

## Objective

Implement `docs/CODEX_MASTER_PLAN.md` Issue 002: SQL migration framework and core enums/constrained fields.

## Research

- Queried node-postgres documentation for Pool use and transaction handling.
- Checked PostgreSQL documentation for enum creation and `INSERT ... ON CONFLICT`.

## Design Decision

Use SQL files as source of truth and a minimal TypeScript runner with node-postgres. Use constrained `text` fields instead of PostgreSQL enum types for early lifecycle/status values.

## Implementation Notes

- Added `packages/db` with a SQL migration runner, seed runner, and CLI scripts.
- Added `infra/db/migrations/0001_initial_schema.sql` for boards, minimal development sources, and raw entries.
- Added constrained text fields for lifecycle status, processing stage, rights status, and failure type.
- Added `infra/db/seeds/dev.sql` with five MVP boards, five development RSS sources, and five development raw entries.
- Kept Source Policy, RSS fetching, extraction, AI, search, reader UI, and admin UI out of scope.

## Verification

- `pnpm --filter @reno-news/db lint`
- `pnpm --filter @reno-news/db test`
- `pnpm --filter @reno-news/db test:integration`
- `DATABASE_URL=postgres://reno_news:reno_news@localhost:5432/reno_news_issue002_test pnpm db:migrate`
- `DATABASE_URL=postgres://reno_news:reno_news@localhost:5432/reno_news_issue002_test pnpm db:seed`
- Re-ran the seed command against the same database to verify idempotence.
- Queried the test database and verified 5 boards, 5 sources, 5 raw entries, and 1 applied migration.
- Verified invalid lifecycle status, processing stage, rights status, and failure type values are rejected.
- `pnpm install --frozen-lockfile`
- `pnpm lint`
- `pnpm test`
- `pnpm build`
- `uv run python -m unittest discover -s tests`
- `docker compose -f infra/compose/compose.yml up --build -d`
- Direct health checks for web, API, and worker.
- Caddy proxy health checks for API and worker.
