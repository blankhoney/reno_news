# 2026-05-20 Milestone 0 Log

## Objective

Start Milestone 0 / Issue 001 from `docs/CODEX_MASTER_PLAN.md`.

## Assumption

The unresolved bootstrap choice is settled as `pnpm + uv` for this task unless the user overrides it.

## Research

- Queried current Next.js Route Handler docs for the `/healthz` route shape.
- Queried current Fastify testing docs for public route verification with `inject()`.
- Queried current uv docs for project config and `uv run` behavior.
- Checked current Docker Compose, Caddy, and GitHub Actions official docs for local service orchestration and CI skeleton conventions.

## Review Before Coding

The plan remains within Issue 001 because it creates only infrastructure, health checks, docs, and CI. RSS, crawling, extraction, AI, search, and reader/admin feature logic remain deferred.

## Implementation Notes

- Created root `pnpm` workspace config and TypeScript base config.
- Created `apps/web` with a minimal Next.js page and `GET /healthz`.
- Created `apps/api` with a minimal Fastify app and `GET /healthz`.
- Created `services/worker` with a standard-library Python HTTP health server and `uv` project config.
- Created `packages/contracts`, `packages/ui`, and `packages/config` placeholders.
- Created Dockerfiles, `infra/compose/compose.yml`, and `infra/compose/Caddyfile`.
- Created `.github/workflows/ci.yml`, `README.md`, `.dockerignore`, and health API docs.
- Added ADR-0006 for the `pnpm + uv` bootstrap decision.

## Verification

- `pnpm install` passed.
- `pnpm install --frozen-lockfile` passed.
- `pnpm lint` passed.
- `pnpm test` passed.
- `pnpm build` passed.
- `uv run python -m unittest discover -s tests` passed.
- `docker compose -f infra/compose/compose.yml up --build -d` built and started services.
- `docker compose -f infra/compose/compose.yml ps` showed web, API, worker, PostgreSQL, and Redis healthy.
- `curl -fsS http://localhost:3000/healthz` returned `{"status":"ok","service":"web"}`.
- `curl -fsS http://localhost:3001/healthz` returned `{"status":"ok","service":"api"}`.
- `curl -fsS http://localhost:3002/healthz` returned worker health JSON.
- `curl -fsS http://localhost:8080/api/healthz` and `curl -fsS http://localhost:8080/worker/healthz` passed through Caddy.

## Review After Coding

- Close-read the web health route/test, API app/server/test, worker server/test, Compose file, Caddyfile, CI workflow, Dockerfiles, and README.
- Fixed Caddy's dependency ordering so the worker proxy waits for worker health.
- Fixed the worker Dockerfile to copy `uv.lock`.
- Added ignore rules and cleaned generated TypeScript and Python build metadata.
- Re-ran local checks and Compose health verification after the fixes.

## Commit

- `Initialize Milestone 0 foundation`
