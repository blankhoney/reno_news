# PostgreSQL First Search And No Default Heavy Archive Service

MVP search and related-content capability will start inside PostgreSQL rather than adding Meilisearch, OpenSearch, or a default ArchiveBox path. This preserves the 4C8G single-host budget and keeps indexes and archives secondary to the product database until search experience or archival requirements justify a separate service.
